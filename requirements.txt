# =========================================
# Core Framework (核心框架: 状态机与编排)
# =========================================
langgraph>=0.2.0        # 用于构建 Gather->Analyst->... 的状态机图
langchain>=0.2.0        # 用于 Journalist 模块的 Prompt 和 Chain 管理
langchain-openai>=0.1.0 # 用于连接 DeepSeek/OpenAI 模型
openai>=1.30.0          # DeepSeek API 的底层客户端

# =========================================
# Data Gathering (数据采集: 搜索与爬虫)
# =========================================
duckduckgo-search>=6.1.0 # DuckDuckGo 搜索接口 (需保持较新版本以适配 API 变更)
requests>=2.31.0         # 用于下载网页 HTML
beautifulsoup4>=4.12.0   # 用于解析 HTML 提取正文

# =========================================
# Data Validation (数据校验)
# =========================================
pydantic>=2.7.0          # 用于定义 RawArticle, NewsReport 等数据模型

# =========================================
# UI & Visualization (终端交互)
# =========================================
rich>=13.7.0             # 用于漂亮的终端输出、表格、面板和进度条

# =========================================
# Environment (环境配置)
# =========================================
python-dotenv>=1.0.1     # 用于加载 .env 文件中的 API Key